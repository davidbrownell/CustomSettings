Custom commands used with [poethepoet](https://poethepoet.natn.io/index.html).

## Installation Instructions
1. Set `POETHEPOET_CONFIG` in `run.cmd`.
2. Ensure that `run.cmd` is on the path, or copy the file to a location that is on the path.
